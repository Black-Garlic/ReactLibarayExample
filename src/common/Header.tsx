import React from "react";
import { Layout } from "antd";

const { Header: HeaderLayout } = Layout;

const Header = () => {
  return <HeaderLayout style={{ padding: 0 }} />;
};

export default Header;
